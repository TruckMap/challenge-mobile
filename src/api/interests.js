import interests from './data/interests.json';

/*
Generic boilerplate, but assume that fetching/searching interests is asynchronous
*/

export default {
  getInterest(id) {
    return new Promise((resolve) => {
      // TODO?
    }); 
  }
  getInterests() {
    return new Promise((resolve) => {
      // TODO?
    });
  }
}